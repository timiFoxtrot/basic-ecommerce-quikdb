export enum ROLES {
  ADMIN = "admin",
  USER = "user",
}

export enum SCHEMA {
  USER = "UserSchema",
  PRODUCT = "ProductSchema",
}

export enum STATUS {
  PENDING = "pending",
  APPROVED = "approved",
}
